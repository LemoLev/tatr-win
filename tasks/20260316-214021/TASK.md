# Web interface?

- STATUS: OPEN
- PRIORITY: 30

Similar to how I do that in tore.

```console
$ tatr serve
Listening to http://localhost:6969
```

Open up `http://localhost:6969` in a browser and it's just dynamically
rendering all the tasks as HTML pages and what not.
