# `tatr ls` should print how many tasks were filtered out

- STATUS: OPEN
- PRIORITY: 100
- TAGS:

But it should be probably within the same status. If we viewing in "OPEN" mode we should not print how many "CLOSED" tasks were filtered out.
