# Make the grep command configurable for `tatr-ref`

- STATUS: OPEN
- PRIORITY: 100
- TAGS:

Maybe some sort of a config file in home folder. It must be per user, not per repo. You may need to use https://github.com/tsoding/shlex.h for this.
