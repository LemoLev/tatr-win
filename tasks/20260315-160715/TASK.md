# Build a graph of issues crossreferring to each other

- STATUS: OPEN
- PRIORITY: 100
- TAGS:

Basically analyze TASK.md-s fully and see what other tasks they refer
to and dump a .dot file. Sounds like a fun stream idea.

---

We implemented some sort of command for that on the stream. But it's
currently useless. Not sure what to do with it. It shows only the ids.
It would be nice to see what's behind them like title or what not. But
the titles are usually too big to fit in a nice compact picture.
