# Add `-t` flag to `new` command

- STATUS: CLOSED
- PRIORITY: 100

Should basically act the same as in `summary` or `ls`, but the list of
tags denotes what to put into the `TAGS` of a newly created task.

---

Added `-t` flag for tags and `-p` flag for setting the priority.
