# A command to search referers of a task

- STATUS: CLOSED
- PRIORITY: 100
- TAGS:

Call an external grepping tool. Should probably introduce some sort of
config file system to configure the grepping tool command line.

---

`task ref`

---

I moved the configuration part to TASK(20260901-051338).
