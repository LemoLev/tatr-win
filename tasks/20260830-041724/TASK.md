# Mass rename a tag

- STATUS: OPEN
- PRIORITY: 100
- TAGS:

Some command that makes it possible to rename a tag. It should update
all the relevant tasks and the `tasks/tags` file.

Probably needs to be implemented after TASK(20260828-211200) because
it will introduce the task updating machinery.
